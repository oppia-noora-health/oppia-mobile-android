<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Toolbar at the top -->
    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar" />

    <!-- Badge Notification - directly below toolbar -->
    <LinearLayout
        android:id="@+id/layout_badge_notification"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbar"
        android:orientation="horizontal"
        android:background="@color/oppia_secondary_light"
        android:padding="8dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/tv_badge_message"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            android:text="You've earned a badge!" />

        <ImageButton
            android:id="@+id/btn_dismiss_badge"
            android:layout_width="12dp"
            android:layout_height="20dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_close"
            android:scaleType="centerInside"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/close"
            android:padding="0dp"
            app:tint="@android:color/black" />
    </LinearLayout>

    <!-- Course Sections below badge notification -->
    <FrameLayout
        android:id="@+id/view_course_sections"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layout_badge_notification">

        <org.digitalcampus.oppia.utils.ui.ExpandableRecyclerView
            android:id="@+id/recycler_course_sections"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:cacheColorHint="#00000000"
            android:divider="@color/list_divider"
            android:foreground="?android:windowContentOverlay"
            android:orientation="vertical"
            app:layoutManager="LinearLayoutManager"
            tools:listitem="@layout/row_course_index_section_item" />

        <LinearLayout
            android:id="@+id/loading_course"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical">

            <ProgressBar
                android:id="@+id/progressBar2"
                style="?android:attr/progressBarStyleLarge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal" />

            <TextView
                android:id="@+id/textView"
                style="@style/SmallTextDark"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/loading_course" />
        </LinearLayout>

    </FrameLayout>

    <!-- Media scan -->
    <org.digitalcampus.oppia.utils.ui.MediaScanView
        android:id="@+id/view_media_scan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/layout_badge_notification" />
</RelativeLayout>